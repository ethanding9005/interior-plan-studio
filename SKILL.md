---
name: interior-plan-studio
description: GPT-image2 室内应用提示词库——把户型图/平面图/手绘草图一键转换为专业室内设计图。支持 8 种输出：写实材质平面图、手绘图转 CAD 风格线稿、空户型家具布置图、空白平面功能布局（咖啡厅等业态）、家具尺寸与人体尺度分析图、9 页方案汇报 PPT 九宫格、三色动线分析图、空间区域 4K 实景效果图。当用户上传户型图、平面布置图、平面图、手绘平面草图、毛坯图，并提出"生成效果图/材质图/分析图/动线图/汇报 PPT/CAD 线稿/家具布置/平面布局"等需求，或提到"室内设计、户型图效果、GPT-image2、室内应用"时使用。
---

# GPT-image2 室内应用

将用户上传的户型/平面图转换为 8 种专业室内设计图。核心是**图生图 + 锁定底图**，全部通过生图工具完成，无需脚本。

## 工作流

1. **确认输入**：用户须提供底图（户型图/平面图/手绘图）。没有底图时要求先上传，本技能不做纯文字生图。
2. **确认模式**：根据用户意图从 8 个模板中选一个；不明确时列出 8 个模式让用户选（见下表）。可多选，多选时逐个出图。
3. **读模板**：从 `references/prompts.md` 读取对应模板，替换 `{变量}`（材质、风格、业态、视角等），未指定的变量用模板默认值。
4. **调生图（图生图）**：
   - `image1` = 用户底图；如有风格参考图则作 `image2`
   - `input_fidelity` = high（最大程度保持原图布局与结构）
   - `size` 按底图宽高比就近取（横图 1536x1024，竖图 1024x1536，方图 1024x1024），`quality` = high
5. **迭代修改**：用户反馈局部问题时，用**上一版生成图**作为 image1，提示词写"保持所有内容不变，仅修改：{问题}"，不要重跑底图。
6. 交付前提醒用户：生图消耗积分；AI 生成图中文字可能不可读，PPT 汇报类输出如需真实可编辑文件需另行走 PPT 流程。

## 8 个模式速查

| # | 模式 | 适用输入 | 一句话 |
|---|------|----------|--------|
| 1 | 材质平面图 | 任意户型图 | 俯视材质铺装渲染，可自定义各空间地面材质 |
| 2 | 手绘转 CAD 线稿 | 手绘草图/照片 | 矫正歪斜线条，输出黑白 CAD 风格标注线稿 |
| 3 | 空户型家具布置 | 有房间名无家具的空户型 | 按人体工程学布置家具，视觉平面图形式 |
| 4 | 空白平面功能布局 | 空白建筑平面 | 保留结构做业态功能布局（默认咖啡厅，可换） |
| 5 | 家具尺寸与人体尺度分析图 | 户型平面图 | 黑白银灰，尺寸引线 + 人体模型 + 图例注释 |
| 6 | 9 页方案汇报 PPT | 户型平面图 | 3×3 九宫格：封面/目录/解析/理念/主卧/公共区/效果/致谢/延展 |
| 7 | 三色动线分析图 | 户型平面图 | 红/绿/蓝虚线动线 + 半透明功能色块 + Legend 图例 |
| 8 | 空间实景效果图 | 户型平面图（可配红框） | 指定区域 4K 人视实景渲染（默认客厅，可换） |

## 通用规则

- 模式 5/6/7 类分析图：不改变原图空间结构与比例，家具位置不变。
- 模板里 `{...}` 为变量；用户没说的不要擅自加戏，用默认值并在交付时说明可改。
- 用户以截图方式给参考版式时，作为 image2 传入并在提示词中注明"参考第二张图的版式/标注风格"。
- 底图是手机截图/照片时，提示词追加"以图片中的平面图部分为底图"。
